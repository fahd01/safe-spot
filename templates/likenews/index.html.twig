{% extends 'base.html.twig' %}

{% block title %}Likenews index{% endblock %}

{% block body %}
    <h1>Likenews index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Id_user</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for likenews in likenews %}
            <tr>
                <td>{{ likenews.id }}</td>
                <td>{{ likenews.idUser }}</td>
                <td>
                    <a href="{{ path('app_likenews_show', {'id': likenews.id}) }}">show</a>
                    <a href="{{ path('app_likenews_edit', {'id': likenews.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_likenews_new') }}">Create new</a>
{% endblock %}
