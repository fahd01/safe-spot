{% extends 'base.html.twig' %}

{% block title %}Likenews{% endblock %}

{% block body %}
    <h1>Likenews</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ likenews.id }}</td>
            </tr>
            <tr>
                <th>Id_user</th>
                <td>{{ likenews.idUser }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_likenews_index') }}">back to list</a>

    <a href="{{ path('app_likenews_edit', {'id': likenews.id}) }}">edit</a>

    {{ include('likenews/_delete_form.html.twig') }}
{% endblock %}
